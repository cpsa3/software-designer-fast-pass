# 软件设计师核心算法原理全景图解指南

> **所属模块**：软考中级《软件设计师》—— 下午试题四（15分算法核心大题）
> **定位说明**：本附录采用原生 Mermaid 代码构建四大核心算法原理的心智模型图解，100% 纯代码矢量渲染，零外部图片依赖，支持离线与跨平台全兼容。

---

## 图一：分治法（Divide & Conquer）——“分-治-合”倒树模型

分治法的灵魂是：**大化小、小化了、自底向上拼结果**。其核心前提是**各子问题相互独立且与原问题结构相同**。

```mermaid
flowchart TD
    subgraph Divide ["① 分解阶段（Divide）：自顶向下递归切分"]
        P["原问题 P(规模 n)"] --> P1["子问题 P1 (规模 n/2)"]
        P --> P2["子问题 P2 (规模 n/2)"]
        P1 --> P11["子问题 P1.1 (规模 n/4)"]
        P1 --> P12["子问题 P1.2 (规模 n/4)"]
        P2 --> P21["子问题 P2.1 (规模 n/4)"]
        P2 --> P22["子问题 P2.2 (规模 n/4)"]
    end

    subgraph Conquer ["② 求解阶段（Conquer）：到达递归基直接求解"]
        P11 --> S11["基准解 S1.1"]
        P12 --> S12["基准解 S1.2"]
        P21 --> S21["基准解 S2.1"]
        P22 --> S22["基准解 S2.2"]
    end

    subgraph Combine ["③ 合并阶段（Combine）：自底向上两两合并"]
        S11 & S12 --> S1["合并解 S1"]
        S21 & S22 --> S2["合并解 S2"]
        S1 & S2 --> S["最终解 S (原问题答案)"]
    end

    style P fill:#f9f0ff,stroke:#722ed1,stroke-width:2px
    style S fill:#d9f7be,stroke:#389e0d,stroke-width:2px
```

### 考点速记与复杂度公式
- **三大步骤**：
  1. **Divide（分）**：将大问题切分为若干规模较小、相互独立的子问题。
  2. **Conquer（治）**：若子问题规模足够小（达到递归基），则直接求解；否则继续递归。
  3. **Combine（合）**：将各子问题的解自底向上逐步合并为原问题的解。
- **典型考题**：归并排序（Merge Sort）、快速排序、折半查找。
- **复杂度推导（主定理 Master Theorem）**：
  $$T(n) = 2T(n/2) + O(n) \implies O(n \log n)$$

---

## 图二：动态规划法（DP）—— 0-1背包状态转移与二维填表决策流

动态规划的灵魂是：**重叠子问题 + 最优子结构**。通过建立表格记录已求解的子问题，避免指数级的暴力递归重复计算。

```mermaid
flowchart TD
    Start["计算 dp[i][w]<br>(考虑前 i 个物品，背包容量为 w 时的最大价值)"] --> Cond{"当前物品重量 w_i 是否 > 当前容量 w ?"}

    Cond -- "是（装不下）" --> Case1["策略：不能选第 i 件物品<br>直接继承上一行的最优解"]
    Case1 --> Update1["dp[i][w] = dp[i-1][w]"]

    Cond -- "否（装得下）" --> Case2["面临二选一决策：选 还是 不选？"]
    Case2 --> OptA["方案A（不选）：<br>价值 = dp[i-1][w]"]
    Case2 --> OptB["方案B（选入）：<br>价值 = dp[i-1][w - w_i] + v_i<br>(腾出 w_i 空间的最优解 + 本物品价值)"]
    
    OptA & OptB --> MaxVal["取两者最大值：<br>max(方案A, 方案B)"]
    MaxVal --> Update2["dp[i][w] = max(dp[i-1][w], dp[i-1][w - w_i] + v_i)"]

    style Update1 fill:#fff7e6,stroke:#d46b08,stroke-width:2px
    style Update2 fill:#e6f7ff,stroke:#096dd9,stroke-width:2px
```

### 核心状态转移方程与填表依赖
$$
	ext{DP}[i][w] = egin{cases} 
	ext{DP}[i-1][w], & w < w_i \
\max(	ext{DP}[i-1][w], \; 	ext{DP}[i-1][w - w_i] + v_i), & w \ge w_i
\end{cases}
$$

当你在填算第 $i$ 行、第 $w$ 列时，它的数据来源只有两个格子的指引：

| 行 \ 列 (容量) | ... | $w - w_i$ | ... | $w$ |
| :--- | :---: | :---: | :---: | :---: |
| **第 $i-1$ 行** | ... | **`dp[i-1][w - w_i]`** *(加上 $v_i$ 参与比选)* | ... | **`dp[i-1][w]`** *(直接垂直落下)* |
| **第 $i$ 行** | ... | | ... | $\Downarrow$<br>**`dp[i][w] = max(...)`** |

- **时空复杂度**：时间复杂度 $O(n \cdot W)$，空间复杂度 $O(n \cdot W)$（滚动数组优化后可降至 $O(W)$）。

---

## 图三：贪心法 vs 动态规划法——“抉择分歧”对比图

下午题常考问答：“为什么该问题采用动态规划而不是贪心算法？”

```mermaid
flowchart LR
    subgraph 贪心算法_Greedy ["贪心法（只顾眼前，不可反悔）"]
        G0["当前状态"] --> G1["局部最优选择 1<br>(如优先拿单位价值最高者)"]
        G1 --> G2["局部最优选择 2"]
        G2 --> G3["最终结果<br>(可能陷入局部陷阱，不保证全局最优)"]
    end

    subgraph 动态规划_DP ["动态规划（全局统筹，备忘推导）"]
        D0["当前状态"] --> D1A["保留备选分支 1"]
        D0 --> D1B["保留备选分支 2"]
        D1A & D1B --> D2["通过状态转移方程<br>综合比对子问题最优值"]
        D2 --> D3["最终全局最优解"]
    end

    style 贪心算法_Greedy fill:#fff0f6,stroke:#c41d7f
    style 动态规划_DP fill:#f6ffed,stroke:#389e0d
```

### 经典考例比对（背包问题）
* **部分背包（物品可任意切割）**：可以直接按 $rac{	ext{价值}}{	ext{重量}}$ 从大到小优先装满。**贪心法有效**，复杂度 $O(n \log n)$。
* **0-1 背包（物品不可切，要么全拿要么不拿）**：若贪心拿单价最高者，可能因空间剩余无法装其他物品而导致总价值极低。**贪心法失效，必须使用动态规划**。

---

## 图四：回溯法 vs 分支限界法——解空间树搜索策略全景图

回溯法与分支限界法都用于在**解空间树**中搜索问题的解，但**搜索策略与数据结构截然不同**：

```mermaid
graph TD
    subgraph 回溯法_DFS ["回溯法：深度优先搜索（DFS）+ 剪枝 + 状态撤销"]
        R0((根)) --> R1((1))
        R1 --> R2((2))
        R2 -.->|"遇到死胡同<br>触发剪枝并回退"| R1
        R1 --> R3((3))
        R3 --> R4((解))
    end

    subgraph 分支限界法_BFS ["分支限界法：广度优先搜索（BFS）+ 队列/堆 + 限界剪枝"]
        B0((根)) --> B1((1))
        B0 --> B2((2))
        B0 --> B3((3))
        B1 --> B4((4))
        B1 --> B5((5))
        B2 -.->|"超出界限函数<br>直接舍弃该活节点"| BX((X))
    end

    style R4 fill:#52c41a,stroke:#237804,stroke-width:2px
    style BX fill:#ff4d4f,stroke:#a8071a,stroke-width:2px
```

### 核心考点对照速查表

| 对比维度 | 回溯法（Backtracking） | 分支限界法（Branch & Bound） |
| :--- | :--- | :--- |
| **搜索方式** | **深度优先（DFS）** | **广度优先（BFS）** 或 极小化代价优先 |
| **遍历机制** | 一次只深入一个子节点，走不通回退上一层（试探与回退） | 一次性生成当前活结点的**所有**子节点 |
| **底层数据结构** | **系统调用栈（递归实现）** | **FIFO 队列** 或 **优先队列（大/小顶堆）** |
| **求解目标** | 找出满足约束的**所有解**或任一可行解 | 找出满足目标函数的最优解（极值问题） |
| **典型考题** | N皇后问题、迷宫巡线、八数码 | 0-1背包分支限界、单源最短路径、旅行商(TSP) |

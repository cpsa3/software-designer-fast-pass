# 软件设计师核心算法原理全景图解指南

本指南专为软考中级《软件设计师》下午试题四（15分核心大题）量身打造，图文结合梳理命题率最高的三大核心算法原理。

---

## 1. 分治法（Divide & Conquer）——“分-治-合”全景流程图

分治法的灵魂是：**大化小、小化了、自底向上拼结果**。核心前提是**各子问题相互独立且与原问题结构相同**。

![分治法原理图解：分解(Divide) -> 求解(Conquer) -> 合并(Combine)](/Users/krystal/.gemini/antigravity/brain/542d13a3-f4ff-495d-996d-2e2d14475592/divide_conquer_diagram_1789719396675.jpg)

### 核心考点提炼
- **三大步骤**：
  1. **Divide（分）**：将待处理的大规模问题对半分裂成 $k$ 个独立的子问题。
  2. **Conquer（治）**：当子问题足够小（递归基 Base Case），直接给出解。
  3. **Combine（合）**：将子问题的解自底向上合并，得到原问题的最终解。
- **典型考题**：归并排序（Merge Sort）、快速排序、二分搜索。
- **复杂度速算（主定理 Master Theorem）**：
  $$T(n) = 2T(n/2) + O(n) \implies O(n \log n)$$

---

## 2. 动态规划法（DP）—— 0-1背包状态矩阵与转移决策图

动态规划的灵魂是：**重叠子问题 + 最优子结构**。通过建立二维表格记录已求解的子问题，消灭指数级的重复递归。

![动态规划0-1背包算法图解：状态转移网格与决策箭头](/Users/krystal/.gemini/antigravity/brain/542d13a3-f4ff-495d-996d-2e2d14475592/dp_knapsack_diagram_1789719427257.jpg)

### 核心状态转移方程（下午代码填空必考）
面对第 $i$ 件物品（重量 $w_i$、价值 $v_i$），在当前背包容量限制 $w$ 下：

$$
\text{DP}[i][w] = \begin{cases} 
\text{DP}[i-1][w], & w < w_i \\
\max(\text{DP}[i-1][w], \; \text{DP}[i-1][w - w_i] + v_i), & w \ge w_i
\end{cases}
$$

- **状态转移决策**：
  - **装不下（$w < w_i$）**：直接继承上一行最优解 $\text{DP}[i-1][w]$；
  - **装得下（$w \ge w_i$）**：比较 **方案A（不选该物品：$\text{DP}[i-1][w]$）** 与 **方案B（选入该物品：$\text{DP}[i-1][w - w_i] + v_i$）**，取较大者。
- **读图指引**：
  - **垂直向下箭头（黄色）**：不装入第 $i$ 个物品，值直接来自于上一行同一容量列：`dp[i-1][w]`。
  - **斜向回溯箭头（黄色）**：装入第 $i$ 个物品，向左回退 $w_i$ 容量找到 `dp[i-1][w - w_i]`，再加上当前物品价值 $v_i$。
- **时空复杂度**：时间复杂度 $O(n \cdot W)$，空间复杂度 $O(n \cdot W)$。

---

## 3. 回溯法（Backtracking）—— 解空间树深度搜索与剪枝机制图

回溯法的灵魂是：**一条路走到黑，走不通就撤销选择并返回（试探与回退）**，利用剪枝函数避免无效搜索。

![回溯算法解空间树图解：DFS深度优先遍历、约束剪枝与状态撤销](/Users/krystal/.gemini/antigravity/brain/542d13a3-f4ff-495d-996d-2e2d14475592/backtracking_tree_diagram_1789719463304.jpg)

### 核心考点提炼
- **搜索策略**：**深度优先搜索（DFS）**。
- **剪枝机制（Pruning）**：
  - **约束函数**：检查当前节点是否合法（如八皇后是否在同一直线/斜线上），若不合法直接剪掉（红叉标注）。
  - **状态撤销（Undo State）**：递归返回上一层时，必须恢复现场（例如清空标志位 `visited[i] = 0`），以便探索同级其他分支。
- **与分支限界法的核心对比**：
  - **回溯法**：DFS + 栈（Stack/递归） + 目标是找出所有解/可行解。
  - **分支限界法**：BFS + 队列（FIFO Queue / 优先队列大顶堆） + 目标是寻找单极值最优解。
